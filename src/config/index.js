const host = "http://cms_v3.it-ymz.cn";
const base = host;

export default {
    base,
    host        : `${host}`,
    base_url    : `${base}`,
    img_path    : `${host}/uploads/images`,
    file_path   : `${host}/uploads/files`,
}
